//Crie um script que declare uma variável e verifique 
//se o seu valor é um booleano. Se for, exiba a mensagem "É um booleano", 
//caso contrário, exiba a mensagem "Não é um booleano".

let myVar = false;

if (typeof myVar === 'boolean') {
  alert("Is a boolean")
}
else {
  alert("not a boolean")
}