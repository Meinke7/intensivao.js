//Crie um script que declare uma variável e verifique se o seu valor é 
//uma string. Se for, exiba a mensagem "É uma string", 
//caso contrário, exiba a mensagem "Não é uma string".

let myVar = "5";

if (typeof myVar === 'string') {
  alert("is a string")
}
else {
  alert("not a string")
}